# Secrets — never commit
.env
secrets/*

# Local show config (copy from config/config.example.json)
config/config.json
!secrets/.gitkeep

# Runtime data (offline sheet cache, logs)
data/*
!data/.gitkeep
*.log

# Node
node_modules/
npm-debug.log*

# OS / editor noise
.DS_Store
Thumbs.db
