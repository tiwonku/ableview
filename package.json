{
  "name": "ableview",
  "version": "0.1.0",
  "description": "Behind-the-scenes now-playing board for a live show: watches the master Ableton session, matches the playing clip to a Google Sheet of cue notes, and pushes role-specific operator views in real time.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "sim": "node src/index.js --sim",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "dotenv": "^17.4.2",
    "fuse.js": "^7.4.2",
    "googleapis": "^173.0.0",
    "osc": "^2.4.5",
    "pino": "^10.3.1"
  }
}
